[alias]
xtask = "run -p xtask --"

[build]
# Shared target directory across worktrees.
target-dir = "../.zdx/cargo-target"

[target.aarch64-apple-darwin]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
